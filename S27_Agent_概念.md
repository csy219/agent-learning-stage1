# S27 Agent 概念笔记

## 1. 普通 LLM 调用 vs Agent
- 普通调用：你问 → 模型回答 → 结束
- Agent:你提需求
        ↕
        模型思考：我现在该做什么？
        ↕
        调用工具（查天气/搜索/读文件/算数）
        ↕
        拿到工具结果，再思考
        ↕
        继续调用工具或给出最终回答

## 2. Agent 的五个组成部分
| 组成 | 作用 | 如果没有它 |
| --- | --- | --- |
| 模型 | 做判断、决定下一步 |没人“思考” |
| 工具 | 真正执行动作：搜索、计算、读写文件 | 只能聊天，干不了活 |
| 循环 | 反复“思考→行动→观察” | 只能一步做完，无法处理多步任务 |
| 记忆 | 记住前文和关键信息 |聊两句就忘了，没法多轮 |
| 人机协同 | 高风险动作前暂停等确认 |可能删错文件、发错消息 |

## 3. 一个 Agent 循环的例子
（写“查天气提醒带伞”或你自己想的例子）
一轮 Agent 的过程是：
1. 模型分析需求：需要真实天气数据 → 决定调用天气工具；
2. 程序执行 get_weather("上海", "明天")；
3. 工具返回 {"weather": "rain"}；
4. 模型拿到结果，判断“下雨 → 需要提醒”；
5. 模型输出最终回答：“明天上海有雨，记得带伞。”
## 4. Agent 的边界
- 什么任务适合 Agent：需要模型自己判断下一步、可能调用多个工具的任务，才值得用 Agent；
- 什么任务不该用 Agent：固定流程（比如“上传 PDF → 切块 → 存入向量库”）用普通代码就好；