你完成了一个真正能用的接口/ask:
    用户上传 .txt + 输入问题
        ↓
    FastAPI 接收并校验文件
        ↓
    读取文本内容
        ↓
    把「文档内容 + 问题」交给大模型
        ↓
    模型流式回答，一段段返回给用户
        ↓
    data: [DONE] 结束

    它不是玩具：这个结构就是你以后 PDF 知识库问答、论文 Agent 的原始雏形——只差“解析 PDF、检索资料”两步。

    S20 新出现的知识点
- File(...) + Form(...)：一个接口同时接收“文件”和“普通文字字段”；
- multipart/form 表单：/docs 里能直接选文件；
- 嵌套生成器 generate()：边收模型输出边转发；
- 完整数据流：请求 → 校验 → 读取 → 组装 → 流式响应。