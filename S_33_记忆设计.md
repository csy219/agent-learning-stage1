# S33 记忆设计

## 一、为什么需要记忆

- 模型本身没有记忆，每次请求都是“无状态”的；
- 只靠保留全部历史 → token 会爆、成本变高、响应变慢；
- 只靠裁剪旧消息 → 会失忆（S32 实验已经证明）；
- 所以要把记忆分层：该省的省、该留的留、该检索的检索。

## 二、四种记忆对比

| 类型 | 存什么 | 存哪里 | 怎么取 | 优点 | 缺点 |
| --- | --- | --- | --- | --- | --- |
| 短期窗口记忆 | 最近几轮原始对话 | 内存里的 messages 列表 | 每次请求直接带上 | 简单、准确、上下文连贯 | 超出 token 预算就会被裁剪 |
| 摘要记忆 | 旧对话的压缩总结 | 一条 summary 消息 / 文本文件 | 每次请求都固定带上 | 省 token，能保留长期主线 | 有损，细节会丢 |
| 结构化档案记忆 | 稳定事实：姓名、职业、目标、偏好 | JSON 文件 / SQLite | 按 user_id 读取 | 精准、可靠、便宜 | 只适合字段固定的信息 |
| 向量检索记忆 | 历史片段、文档、知识点 | 向量库（Chroma 等） | 按当前问题相似度检索 top-k | 可存海量、按需召回 | 需要 embedding，可能召回不准 |

## 三、我的组合方案

每次请求发给模型的 messages 结构：

```text
[
  {"role": "system", "content": "你是助手，回答简洁清楚"},
  {"role": "system", "content": "用户档案：姓名=小林；方向=Agent 工程师；偏好=简短回答"},
  {"role": "system", "content": "历史摘要：用户已学完 FastAPI 基础，正在学工具调用与记忆设计"},
  ... 最近 5 轮原始对话 ...,
  {"role": "system", "content": "相关记忆：用户曾提到想先做 PDF 问答项目"},
  {"role": "user", "content": "我接下来该学什么？"}
]
```

核心原则：

1. system（人设 + 规则）永远保留；
2. 用户档案永远保留（很短，但价值最高）；
3. 历史摘要永远保留（控制在一两百字内）；
4. 最近 N 轮原始对话保留（保证连续性和细节）；
5. 相关记忆按需检索（只在与当前问题相关时加入）。

## 四、触发规则

### 1. 短期窗口

- 保留最近 5 轮对话，或保留到接近 token 预算为止；
- 超出预算时，先把最旧对话送去生成摘要，再丢弃原文。

### 2. 摘要记忆

触发条件：历史消息超过 8 条，或估算 token 超过 500。
做法：

- 取出最旧的一半对话；
- 让模型压缩成 100–200 字摘要；
- 摘要必须保留：用户身份、目标、已做决定、未完成事项；
- 用新的摘要替换旧的摘要，再删除被压缩的原文。

摘要提示词示例：

```text
请把下面的对话压缩成一段不超过 150 字的记忆摘要。
必须保留：用户身份信息、学习目标、已经做过的决定、还没完成的事。
不要编造，不要加入对话里没有的信息。

对话内容：
...
```

### 3. 结构化档案

触发条件：用户提到稳定事实，例如姓名、职业、方向、偏好、长期目标。
做法：

- 让模型抽取成 JSON；
- 合并写入 `profile.json`；
- 每次对话都带上这个 JSON。

抽取提示词示例：

```text
从用户的话里抽取稳定事实，输出 JSON，字段为：
name（姓名）、goal（目标）、preference（偏好）、level（当前水平）。
没有提到的字段用 null，不要编造。
```

`profile.json` 示例：

```json
{
  "name": "小林",
  "goal": "成为 Agent 工程师",
  "preference": "回答简短",
  "level": "已学完 FastAPI 基础"
}
```

### 4. 向量检索记忆

触发条件：用户问到过去提过的事、跨会话信息、或需要从大量历史/文档中找相关片段。
做法：

- 把对话片段或文档切块、向量化，存入向量库；
- 当前问题也向量化；
- 检索相似度最高的 3–5 条；
- 设置相似度阈值，低于阈值的不加入上下文。

## 五、取舍与原则

- 越简单的方案越先上：先做短期窗口 + 结构化档案；
- 再加摘要记忆解决“长对话失忆”；
- 最后才上向量检索，因为它最重（embedding、存储、延迟）；
- 记忆不是越多越好，塞太多无关信息反而干扰模型。

## 六、和后续步骤的关系

- S34：实现“摘要记忆 + 检索记忆”的最小版本；
- S38–S43：LangGraph 的 Checkpointer 负责短期状态持久化；
- 项目 A / 项目 B：向量记忆会升级为真正的知识库检索。

## 七、自测

1. 为什么不能只靠“保留全部历史”？
2. 摘要记忆和向量记忆各自解决什么问题？
3. 为什么“姓名、目标”更适合结构化档案，而不是只靠向量检索？
4. 一次请求的 messages 应该包含哪几部分？