# 阶段一复盘

## 完成了什么
- 会调用大模型 API，并支持流式输出
- 会做结构化输出 + Pydantic 校验 + 自动重试
- 会写 FastAPI 接口：上传文件、SSE 流式、文档问答
- 会解析 PDF/Word，会写 pytest 测试
- 完成 CLI 待办小项目


## 下一步目标
进入阶段二：手写 Tool Calling、LangGraph、MCP、评测与两个 Agent 项目。