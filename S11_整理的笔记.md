1. token
模型读写文本的最小单位，不是“一个字”或“一个词”，可能是一段字符被切成的片段。token 决定两件事：上下文能装多少内容、每次调用花多少钱。

2. temperature
控制回答随机性的参数：越低越稳定保守（接近 0），越高越发散有创造性。写代码、解析 JSON 用低值；写文案、头脑风暴可用高值。注意：它不是“聪明程度”，不会让模型更聪明

3. 上下文窗口（context window）
模型一次请求能同时“看到”的输入 + 输出总 token 上限。超出会报错或截断，所以长对话/长文档要裁剪、分块。你现在可以回忆 S08 的 max_tokens=20 被截断，就是“人为缩小窗口”的效果。

4. 幻觉（hallucination）
模型一本正经地编造看起来合理但其实是错的内容，比如编造不存在的文献、数据。它不代表模型“故意骗人”，而是概率生成本身的缺陷。后面 RAG、加引用、约束回答都是为缓解幻觉。

5. 消息角色（message roles）
API 对话里的三种角色：
• system：设定人设与规则；
• user：用户输入；
• assistant：模型之前的回答，用于多轮对话记忆。