你通过这一步额外学到的底层知识
- HTTP 里 GET 和 POST 的区别（取数据 vs 提交数据）；
- 一个 URL 如何对应一段代码（路由）；
- JSON 如何在网络里进出（请求体、响应体）；
- 类型标注和 Pydantic 让接口自动校验、自动出文档；
- “服务端”和“客户端”的概念。
所以 S17 现在看起来只是“回声”，但它其实是：你从“写脚本”升级到“写服务”的第一块砖


用户（浏览器/App）→ 网址请求 → 你的 FastAPI 服务 → 调用大模型 → 把回答返回给用户